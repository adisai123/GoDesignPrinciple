# Builder slowly creates object step by step where as factory creates it in one single invocation ( object creation will be outsourced)
# Ways to implement it in go lang:
    1.  A seperate function(factory function)
    2.   Seperate struct

